let base = 5
let height = 7
const trianguloArea = (base, height) => base * height / 2

console.log(`El area de un triangulo de base
${base} y altura ${height} es: ${trianguloArea(base, height)}`)

const PI = 3.1416
let radio = 5
const areaCirculo = (PI , radio) => PI * (radio*radio)
console.log(`El area de un circulo de radio ${radio}
es igual a ${areaCirculo(PI, radio)}`)